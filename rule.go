package main

import (
	"fmt"
)

func main() {
	wantError := `unknown error:"test"`
	fmt.Println("vim-go", wantError)
}

for _, v := range data {
	if v.F1 != 1 {
		fmt.Println("err")
		continue
	}

	v = dosth()
}
